export default function Header(state){
    return `
      <div id="header">
        <div class="container">
          <h1>${state.title}</h1>
          <em>Simply that Decor</em>
          <strong>Were we Simply do your wedding right</strong>
        </div>
      </div>
    `;
}